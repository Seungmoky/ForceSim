model = "gpt-5.3-codex"

# 변경/명령 실행 시 너무 공격적으로 가지 않게 시작
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# 프로젝트 분석 시 웹 검색 기본 사용 여부는 팀 정책에 맞게 조절
# 최신 문서/패키지 버전 확인이 잦으면 유용
web_search = "cached"

[projects.'\\?\C:\smlee\git_project\ForceSim']
trust_level = "trusted"